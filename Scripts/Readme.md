This folder contains the standalone R scripts for the project.

Notes:
- Scripts are written to run without `setwd()` and without hard-coded local file paths.
- Each script is self-contained and includes comments explaining *why* steps are taken.
- Run from the project root with `Rscript Scripts/<script-name>.r`.
